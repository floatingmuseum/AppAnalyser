<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/ll_item_app"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:paddingTop="16dp"
    android:paddingStart="10dp"
    android:paddingBottom="16dp"
    android:layout_height="100dp"
    android:gravity="center_vertical"
    android:layout_marginTop="2dp"
    android:layout_marginBottom="2dp"
    android:background="@drawable/dash_line_green_background"
    tools:ignore="RtlSymmetry">

    <ImageView
        android:id="@+id/iv_icon"
        android:layout_width="60dp"
        android:layout_height="60dp"
        tools:src="@drawable/ic_icon2"/>

    <LinearLayout
        android:layout_marginStart="20dp"
        android:orientation="vertical"
        android:gravity="center_vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:gravity="center_vertical"
            android:id="@+id/tv_name"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:textColor="@color/text_item_color"
            tools:text="名称:AppAnalyser" />

        <TextView
            android:gravity="center_vertical"
            tools:text="包名:com.floatingmuseum.app.analyser"
            android:id="@+id/tv_package_name"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:textColor="@color/text_item_color"
            android:layout_weight="1" />

        <TextView
            android:gravity="center_vertical"
            tools:text="版本:1.0.99"
            android:id="@+id/tv_ver_name"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:textColor="@color/text_item_color"
            android:layout_weight="1" />
    </LinearLayout>
</LinearLayout>